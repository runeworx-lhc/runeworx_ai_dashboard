<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>⚡ Rune Worx AI Dashboard</title>
  <style>
    body {
      background: #0a0a0a;
      color: #ddd;
      font-family: 'Courier New', monospace;
      margin: 0;
      padding: 0;
    }
    header {
      background: #111;
      padding: 1rem;
      text-align: center;
      color: gold;
      font-size: 1.8rem;
      border-bottom: 2px solid #444;
      text-shadow: 0 0 5px gold;
    }
    #gpt-console {
      margin: 2rem auto;
      max-width: 600px;
      padding: 1rem;
      background: #1a1a1a;
      border-radius: 8px;
      box-shadow: 0 0 10px #222;
    }
    #user-input {
      width: 100%;
      height: 80px;
      background: #222;
      color: #eee;
      border: 2px solid gold;
      padding: 0.5rem;
      font-size: 1rem;
    }
    #send-btn {
      margin-top: 0.5rem;
      padding: 0.5rem 1.5rem;
      background: gold;
      color: #000;
      font-weight: bold;
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }
    #send-btn:hover {
      background: #ffcc00;
    }
    #gpt-output {
      margin-top: 1rem;
      background: #111;
      border: 1px solid #333;
      padding: 1rem;
      min-height: 150px;
      white-space: pre-wrap;
    }
  </style>
</head>
<body>

  <header>⚡ Rune Worx AI Dashboard</header>

  <div id="gpt-console">
    <textarea id="user-input" placeholder="Speak 'Thor’s Welcome' to begin..."></textarea>
    <br />
    <button id="send-btn">Invoke GPT</button>
    <div id="gpt-output">🌀 Awaiting invocation...</div>
  </div>

  <script src="config.js"></script>
  <script>
    document.getElementById('send-btn').addEventListener('click', async () => {
      const text = document.getElementById('user-input').value.trim();

      if (!text.toLowerCase().includes("thor’s welcome")) {
        document.getElementById('gpt-output').textContent = "⚠️ Speak the sacred words: Thor’s Welcome";
        return;
      }

      document.getElementById('gpt-output').textContent = "⚡ Summoning Odin’s insight...";

      try {
        const resp = await fetch(OPENAI_API_URL, {
          method: "POST",
          headers: {
            "Content-Type": "application/json",
            "Authorization": "Bearer " + YOUR_API_TOKEN // Use secure method to store token
          },
          body: JSON.stringify({
            model: "gpt-4o",
            messages: [
              { role: "system", content: "You are Rune Worx's Norse-inspired assistant." },
              { role: "user", content: text }
            ]
          })
        });

        const data = await resp.json();
        document.getElementById('gpt-output').textContent =
          data.choices?.[0]?.message?.content || "⚠️ Something went wrong. Check your invocation.";
      } catch (error) {
        document.getElementById('gpt-output').textContent = "⚠️ Error: " + error.message;
      }
    });
  </script>

</body>
</html>
